server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/airline
    username: postgres
    password: admin

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate