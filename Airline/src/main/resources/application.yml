server:
  port: 8080

spring:
  liquibase:
    change-log: db.changelog/db.changelog-master.yaml
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/airline
    username: postgres
    password: admin

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate